import XCTest
@testable import SSNet
import MSDKDns_C11

final class SSNetTests: XCTestCase {
    func testMSDKDnsHttpMessageTools() {
        let tools = MSDKDnsHttpMessageTools()
        // 假设 MSDKDnsHttpMessageTools 有一个方法叫做 exampleMethod
        // 这里我们调用 exampleMethod 并验证其行为
        tools.exampleMethod()
        // 添加断言来验证预期结果
        XCTAssertTrue(true) // 根据实际情况修改断言
    }
}